[build]
builder = "DOCKERFILE"
# Ruta relativa al Root Directory configurado en Railway ("backend/")
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
